Onyx Kalender FIX v34
=====================

Behobene Punkte:
1) Crash nach Login gefixt:
   - Schutz für leere shoppingListsCloud Arrays
   - normalized[0].id -> normalized[0]?.id
   - next[0].id -> next[0]?.id

2) Wetter-Absicherungen:
   - hourlyForecast?.time.length -> hourlyForecast?.time?.length || 0
   - userProfile?.weatherLocation.name -> userProfile?.weatherLocation?.name
   - userProfile?.weatherLocation.lat -> userProfile?.weatherLocation?.lat
   - userProfile?.weatherLocation.lon -> userProfile?.weatherLocation?.lon

3) Chat-Härtung:
   - Chats aus Firestore werden vor setMyChats bereinigt
   - activeChat / participants robuster abgesichert
   - getProfile mit Null-Schutz

4) UI-Fallback:
   - ErrorBoundary um die Hauptansicht gelegt, damit nicht mehr nur schwarzer Screen erscheint

5) Cache / Build:
   - BUILD_VERSION auf v34 erhöht
   - Service Worker Cache auf onyx-v38 erhöht

Hinweis:
- Diese ZIP ist für Repo/GitHub-Push gedacht.
- Dist wurde hier nicht neu gebaut; nach Push sollte GitHub Actions / Vite neu bauen.
- Danach im Browser / Android WebView Service Worker leeren bzw. hart neu laden.
